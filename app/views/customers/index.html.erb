<div class="container-fluid">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
      <span class="sr-only">Menu de Navegacion</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
  </div>
</div>
<div class="container-fluid">
  <div class="row">
    <div class="col-sm-3 col-md-2 sidebar">
    </div>
    <div class="col-sm-offset-2 col-md-9 col-md-offset-3">
<h1>Listado de Clientes</h1>

<table class="table">
  <thead>
    <tr>
      <th><%= Customer.human_attribute_name(:name) %></th>
      <th><%= Customer.human_attribute_name(:lastname) %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @customers.each do |customer| %>
      <tr>
        <td><%= customer.name %></td>
        <td><%= customer.lastname %></td>
        <td><%= link_to 'Historial', store_customers_path(customer) %></td>
        <td><%= link_to 'Editar', edit_customer_path(customer) %></td>
        <td><%= link_to 'Eliminar', customer, method: :delete, data: { confirm: 'Estás seguro?' } %></td>
        <td><%= link_to 'Pago', new_customer_current_account_path(customer) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= link_to 'Nuevo Cliente', new_customer_path %>
</div>
</div>
</div>
